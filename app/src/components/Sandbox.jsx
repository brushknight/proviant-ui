import * as React from 'react'
import Button from './generic/Button'

const Sandbox = () => {
	return (
		<section>
			<Button text={'Какая то кнопка'} icon={'plus'}/>
		</section>
	)
}

export default Sandbox
